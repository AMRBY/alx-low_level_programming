#include <ctype.h>

/**
 * mul - find digit
 * Description: find digit
 * @a: int
 * @b: int
 * Return: 0/1
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);

}
int add(int a, int b)
{
        int c;

        c = a + b;
        return (c);

}
int sub(int a, int b)
{
        int c;

        c = a - b;
        return (c);

}
int div(int a, int b)
{
        int c;

        c = a / b;
        return (c);

}
int mod(int a, int b)
{
        int c;

        c = a % b;
        return (c);

}
