#include "main.h"

/**
 * swap_int - 98
 * Description: find digit
 * @a: int pointer
 * @b : int pointer
 * Return: no
 */
void swap_int(int *a, int *b)
{
	int tmp;

	tmp = *a;
	*a = *b;
	*b = tmp;
}

