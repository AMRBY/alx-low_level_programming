#include <string.h>
#include "main.h"

/**
 * _strlen - length of strings
 * Description: find digit
 * @s: int pointer
 * Return: 0
 */
int _strlen(char *s)
{
	int v;

	v = strlen(s);
	return (v);
}

